import { FixCategoryPipe } from "./fix-category.pipe";

describe("FixCategoryPipe", () => {
  it("create an instance", () => {
    const pipe = new FixCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
